[package]
name = "open-space"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.15.3"

winit = "0.30.5"
tokio = { version = "1.41.0", features = ["full"] }
core-graphics2 = { version = "0.5.2", default-features = false, features = [
  "display",
  "objc",
  "window",
] }
core-media = { version = "0.5.1", default-features = false, features = [
  "objc",
] }
core-video = { version = "0.4.3", features = ["objc"] }
objc2 = { version = "0.5.2", features = ["relax-sign-encoding"] }
objc2-foundation = { version = "0.2.2", features = [
  "NSArray",
  "NSDictionary",
  "NSError",
  "NSGeometry",
  "NSString",
] }
dispatch2 = "0.3.1"
core-foundation = { version = "0.10.1", default-features = false }
rand = "0.8.5"
libc = "0.2.161"
screen-capture-kit = "0.3.1"
ar-drivers = "0.4.3"
ahrs = "0.8.0"
nalgebra = "0.34"
serde_json = "1"

[dev-dependencies]
cargo-husky = { version = "1", default-features = false, features = ["user-hooks"] }

[workspace]
resolver = "2"

# Enable optimization in debug mode
[profile.dev]
opt-level = 1

# Enable high optimizations for dependencies (incl. Bevy), but not for our code:
[profile.dev.package."*"]
opt-level = 3
